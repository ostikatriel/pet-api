##########################################################
# 🔧 CONFIGURACIÓN DE ENTORNO (.env)
# Este archivo define las variables necesarias para ejecutar el proyecto.
# Copiar este archivo y renombrarlo como `.env`, luego ajustar según tu entorno.
##########################################################

##########################################################
# 🐳 CONFIGURACIÓN DE BASE DE DATOS
##########################################################

# Si estás ejecutando PostgreSQL mediante Docker,
# usa el puerto configurado en tu docker-compose.yml (por ejemplo: 5433).
# Ejemplo de conexión Docker:
# DATABASE_URL=postgres://postgres:postgres@localhost:5433/petsdb

# Si tienes PostgreSQL instalado directamente en tu PC (instalación local),
# usa el puerto por defecto de PostgreSQL (5432).
# Ejemplo de conexión local:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/petsdb

# 👉 IMPORTANTE:
# Solo una de las dos líneas debe estar activa (sin el símbolo # al inicio).
# Descomenta la que corresponda a tu entorno actual:

DATABASE_URL=postgres://postgres:postgres@localhost:5432/petsdb
# DATABASE_URL=postgres://postgres:postgres@localhost:5434/petsdb

##########################################################
# 🔑 CONFIGURACIÓN DE AUTENTICACIÓN
##########################################################

# Clave secreta usada para firmar y verificar los tokens JWT.
# En producción se recomienda usar una clave más compleja.
JWT_SECRET=supersecretkey

##########################################################
# 🌐 CONFIGURACIÓN DEL SERVIDOR
##########################################################

# Puerto en el que se ejecutará el servidor backend (NestJS/Node.js)
PORT=3000
